# Razorpay Configuration
# Add these to your .env file

# Razorpay Test Credentials (for development)
REACT_APP_RAZORPAY_KEY_ID=rzp_test_YOUR_KEY_ID
RAZORPAY_KEY_SECRET=YOUR_SECRET_KEY

# Razorpay Live Credentials (for production)
# REACT_APP_RAZORPAY_KEY_ID=rzp_live_YOUR_LIVE_KEY_ID
# RAZORPAY_KEY_SECRET=YOUR_LIVE_SECRET_KEY

# Webhook Secret (for verifying webhook signatures)
RAZORPAY_WEBHOOK_SECRET=YOUR_WEBHOOK_SECRET

# Other Configuration
NODE_ENV=development
PORT=3001

# Instructions:
# 1. Go to https://razorpay.com/ and create an account
# 2. Navigate to Dashboard → Settings → API Keys
# 3. Generate Test/Live API keys
# 4. Replace the placeholder values above with your actual keys
# 5. For webhooks, go to Dashboard → Settings → Webhooks
# 6. Add webhook URL: https://yourdomain.com/api/razorpay-webhook
# 7. Copy the webhook secret

# Security Notes:
# - Never commit actual API keys to version control
# - Use test keys during development
# - Only use live keys in production
# - Keep your secret key secure and never expose it in frontend code
